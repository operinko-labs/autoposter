# Sweep 4 — the Test/CI Hygiene Family — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the seven test/CI hygiene rows (107, 109, 118, 131, 142, 152, 167) plus the accumulated cheap frontend DEFER homes that pass the one-liner judgment (M6, and two of the GroupsPanel trio), so the suite and CI guard themselves as well as they guard the product.

**Architecture:** Four tasks. Task 1 lands the guards — the SCHEDULED_JOB_NAMES agreement test (RED against a real drift this plan found: `credits_scan` is already missing from the allowlist), the Library search+filter composition test, the example-config reverse schema check, and the widened root-level ruff gate. Task 2 lands the cleanups — row 142's three independent fixes, row 152's module rename in its own commit, and row 131's measured timeout re-derivation. Task 3 lands the frontend one-liners — M6's fieldErrors join plus the two GroupsPanel trio items that survive the one-liner judgment (the `useState("orig")` seeds and the badge-distance move; the aspect-ratio reflow does NOT and stays homed). Task 4 wraps: rows close, PR body written gitignored, full suites stated-then-measured. NO push, NO PR.

**Tech Stack:** Python 3.14 + FastAPI + SQLAlchemy + pytest (compose `test` service), vitest + React Testing Library via the compose `web` service, ruff, Forgejo CI (`.forgejo/workflows/ci.yml`).

**Binding requirements:** `.superpowers/sdd/p-sweep4-facts.md` (C1–C4, adjudicated 2026-08-31). Where this plan and that file could ever disagree, that file wins.

## Global Constraints

Every task's requirements implicitly include this section.

1. **Scope (facts C1).** Rows IN: 107, 109, 118, 131, 142, 152, 167 — plus M6 (the `fieldErrors` same-path collapse, `frontend/src/api/overrides.ts`, shared by four panels) and the GroupsPanel trio items this plan judged one-liners (M1 `useState("orig")` seeds and M5 badge distance are IN; M3 aspect-ratio reflow is OUT — it needs the upstream grid's true ratio measured and a reserved box the fallback note shares, which is a layout-design decision, not a one-liner; it stays homed at the divider review's "UI polish pass"). OUT: everything touching runtime behavior beyond 142's cleanups (the two disclosed behavioral deltas in this plan — `credits_scan` joining the run-now allowlist and preview counts consulting ownership — are the rows' own remedies, disclosed in their closes); rows 213/214 (redaction work); row 121 (END-placed); the correctness families.
2. **RED-first for every guard (facts C2 — the law).** Every guard added must be SEEN red before it is trusted: a guard that lands against already-correct code is proven by a temporary mutation of the code it guards (run red, restore, run green) — a guard never seen red proves nothing, the run's own repeated lesson. Row 131's numbers are MEASURED from this week's logs, never guessed. Row 152's rename lands git-follow-friendly: one rename, one commit.
3. **Container discipline (the standing recipe).** Unique compose project per task (`psw41`, `psw42`, `psw43`, `psw44`), always with the `.superpowers/isolated-db.yml` overlay for the `test` service, always `sh -c` with tee to a path under `/app/.superpowers/` inside the container — never rely on streamed stdout (it is filtered, and `--rm` deletes the container before it can be re-read). A long run (the full suite) uses **no `--rm`**, is started detached with `run -d --name <project>-<label>`, is waited on from the host, and the log is read from the host. Teardown is `docker compose -p <project> down` — **never** `down -v`. Two pytest commands against the same compose project are unsafe — keep unique `-p` names per task. Execution is main-tree; if a worktree is nonetheless used, any `run-*.log` written inside it is copied out BEFORE the worktree is pruned.
4. **Frontend tests via the compose `web` service with host-side redirect:** `docker compose -p <proj> run --name <proj>-<label> web npm test > .superpowers/run-p-sweep4-<label>.log 2>&1`, then read the log from the host and `docker rm <proj>-<label>`. The tsc gate the same way with `npx tsc --noEmit`.
5. **Timeouts (row 131's own derivation, used by this plan).** Full backend suite: `timeout -s KILL 2700`. Targeted files: `timeout -s KILL 600`. Derived in Task 2 from this week's measured runs (worst full run 878.87s).
6. **Commits.** Conventional messages, `--no-gpg-sign`, staged **by name** (never `git add -A`). No `Co-Authored-By`, no AI attribution of any kind, in commits or the PR body.
7. **Artifacts.** Phase artifacts under `.superpowers/sdd/` use the `p-sweep4-` prefix; run logs are `.superpowers/run-p-sweep4-*.log`. The PR body goes to `.superpowers/sdd/p-sweep4-pr-body.md` — `.superpowers/` is gitignored and the file stays uncommitted. **Push and PR creation are NOT in this plan**: the user gates the PR.
8. **Branch (facts C4).** `chore/sweep-4` from `fix/sweep-3`'s tip (98ceb96 = pending PR #108's content) — THIRD in the stacked merge order (main ← #103-era history ← #108 ← this), which is second-to-merge-rebases *squared*: when #108 rebases onto main, this branch rebases onto the rebased #108, and the wrap's PR body says so. Verified with a **content probe** (never a sha probe): `grep -q '_URL_HOST' src/autoposter/api/logs.py` must succeed on the cut branch — `_URL_HOST` exists only in #108's content, and every line number in this plan was verified against 98ceb96.
9. **Paperwork (facts C3).** The rows close (or re-file per evidence); the PR body plain; the wrap files nothing new unless a review demands it.

## Adjudications Made by This Plan

Decisions the facts file left to the plan-writer, settled here so no executor has to guess. Each is disclosed again in its row's close (Task 4).

- **107 — the guard is RED against the tree today, for real.** Six `Job(name=...)` literals exist — five in `scheduler/jobs.py` (`collections_reconcile`, `ratings_drift_sweep`, `credits_scan`, `arr_sync`, `asset_cleanup`) plus `scheduler/prune.py`'s `plex_prune`; `api/routes.py:77`'s `SCHEDULED_JOB_NAMES` spells only five — `credits_scan` (registered as a real scheduled job at `app.py:296`) is missing, so the row's predicted failure has already happened: the credits scan cannot be hand-triggered. The fix is the row's own remedy — add `credits_scan` to the allowlist (a small, additive behavior change: `POST /api/scheduled-runs/credits_scan/run` stops 404ing). No frontend change: the dashboard renders served rows, and its one hardcoded name (`Collections.tsx:30`) is its own Diff-now button.
- **109 — RED by mutation.** The composition is correct today, so the guard's red is manufactured: temporarily drop `searchQuery` from the listing effect's dependency list (`Library.tsx:201`) — the exact dep-list regression the row fears — watch the new test fail, restore, watch it pass.
- **118 — the gate widens; the row's stale premise is completed, not re-litigated.** The row's cited F401s no longer exist and `ruff check .` is clean at this branch's HEAD (verified at plan time, ruff 0.15.13), but nothing STRUCTURAL keeps the two commands agreeing — the fix is `ci.yml`'s Lint step running `ruff check .` so the root command IS the gate. RED is a planted scratch violation outside `src`/`tests` proving the current gate blind to it.
- **131 — a documentation re-derivation plus three stale-count refreshes; no timeout constant exists in CI to bump.** `.forgejo/workflows/ci.yml` carries no pytest timeout (the runner's default governs); the `timeout -s KILL` numbers live in the standing recipes the phase plans copy forward. The re-derivation: worst full run this week 878.87s (`run-pdivbr-full.log`), so full-suite guard = **2700** (3.07× worst) and targeted-file guard stays **600** (worst single-file green this week ≈ 106s, 5.6× headroom). The same growth staled three prose counts stated as exact (`ci.yml`'s "2,740 fast + 513 deferred + 11 ImageMagick-gated = 3,264 collected" partition comment, `tests/conftest.py:24`, `tests/test_ci_path_filters.py:385-386`) — refreshed from a measured collect in the same commit, disclosed in the row close as the one widening beyond the row's literal "timeout constants".
- **142(a) — deletion, with its tests.** `definition_config_hash` (`config/schema.py:644-656`, the row's `:388` has drifted) is called nowhere in `src/autoposter`; it goes, along with its three tests in `tests/test_collection_config.py` and the `hashlib`/`json` imports it alone used, and `engine.py:746`'s docstring stops citing it.
- **142(b) — preview consults the same ownership rule the pass does, message-free.** The preview block (`engine.py:645-651`) calls `resolve_collision(..., dry_run=True)` (read-only — `claim_ownership` is behind `dry_run`) and computes `adding`/`removing` only when it answers ok; the collision message is NOT appended there because `reconcile_list_collection` — which runs right after in the same preview pass (preview's only caller passes `dry_run=True`, `api/collections_builders.py:348`) — already reports it, and a second append would double it. Cost disclosed: one `collection.reload()` GET per previewed title that exists in Plex, the same per-title cost the real pass already pays in `lists.py`.
- **142(c) — a failed hub write stores `""` as the hash, so the next pass retries.** `_apply_hub` and `apply_collection_settings` gain an ok flag; the three storing call sites (`lists.py`, `smart.py`, `reconcile.py`'s bucket family) store `wanted` only when settings applied clean, else `""` (never equal to any sha256, so the definition re-reconciles next pass — idempotent writes, hub retried). Deliberate consequence, disclosed: a permanently-Pass-less server whose definition sets `visible_*` retries every pass and reports the same "could not set the hub visibility" action every pass — the operator asked for something the server cannot do, and the report says so until they stop asking.
- **152 — the transport renames to `imdb_graphql.py`.** The module is the GraphQL transport to `api.graphql.imdb.com` serving three roots (list, watchlist, advanced search); `imdb_graphql` describes what it IS rather than its first tenant. The builders module `collections/builders/imdb_lists.py` keeps its name (it holds exactly the two list builders it names). Blast radius enumerated in Task 2 — five code files, one logger-name string, three comment mentions.
- **167 — section level only, `{version}` the sole exemption.** Verified at plan time: `Config.model_fields` minus the example's top-level keys is exactly `{"version"}` (the derived render-version hash, never operator-set). RED via the row's own motivating mutation: delete the `tracearr:` block from the example and watch the new test go red.
- **The GroupsPanel trio judgment (facts C1's "IF the plan judges them one-liners").** M1 (`useState("orig")` at `GroupsPanel.tsx:92-93`) — IN: two literal seeds become `""`; unreachable behind the `groups === null` guard, so no test can see it (cosmetic, disclosed). M5 (badge distance) — IN: the unsaved badge's JSX block moves above the `.groups-style` div, pinned by a DOM-order test seen red before the move. M3 (aspect-ratio reflow) — OUT, stays homed: it requires measuring the upstream contact sheet's real ratio and reworking the fallback note to share the reserved box.

## File Structure

| File | Responsibility | Task |
| --- | --- | --- |
| `docs/superpowers/plans/2026-08-31-sweep-4.md` | **Commit as-is** (this plan) | T1 |
| `tests/test_api_scheduled_runs.py` | **Modify.** Row 107's agreement guard (one test, RED naturally) | T1 |
| `src/autoposter/api/routes.py` | **Modify** (:71-83). `credits_scan` joins the allowlist; comment corrected | T1 |
| `frontend/src/pages/Library.test.tsx` | **Modify.** Row 109's composition test (RED by dep-list mutation) | T1 |
| `tests/test_example_config_matches_schema.py` | **Modify.** Row 167's reverse test (RED by example mutation) | T1 |
| `.forgejo/workflows/ci.yml` | **Modify** (:271-272 Lint; T2 also refreshes the :309-315 partition comment) | T1, T2 |
| `src/autoposter/config/schema.py` | **Modify.** Delete `definition_config_hash` (:644-656) + its two imports | T2 |
| `tests/test_collection_config.py` | **Modify.** Delete the three dead-helper tests + import | T2 |
| `src/autoposter/collections/engine.py` | **Modify.** :746 docstring; preview block :645-651; import `resolve_collision` | T2 |
| `tests/test_builder_engine.py` | **Modify.** Row 142(b)'s preview-ownership test (RED naturally) | T2 |
| `src/autoposter/collections/reconcile.py` | **Modify.** `_apply_hub` (:452-496) + `apply_collection_settings` (:518-537) return ok; bucket store (:1035-1048) gates | T2 |
| `src/autoposter/collections/lists.py` | **Modify** (:370-385). Unpack ok; gate the stored hash | T2 |
| `src/autoposter/collections/smart.py` | **Modify** (:419-432). Same gating | T2 |
| `tests/test_builder_settings.py` | **Modify.** Row 142(c)'s retry test (RED naturally) | T2 |
| `src/autoposter/collections/imdb_lists.py` → `src/autoposter/collections/imdb_graphql.py` | **Rename** (git mv, own commit) | T2 |
| `src/autoposter/collections/builders/imdb_lists.py` | **Modify** (:3, :53). Import + docstring follow the rename | T2 |
| `src/autoposter/collections/builders/imdb_search.py` | **Modify** (:3, :50). Same | T2 |
| `tests/test_builder_imdb_lists.py` | **Modify** (:30). Import follows | T2 |
| `tests/test_builder_imdb_search.py` | **Modify** (:6, :35, :361). Import + logger string follow | T2 |
| `src/autoposter/providers/tmdb_lists.py` | **Modify** (:268, :274). Comment mentions follow | T2 |
| `src/autoposter/providers/tracearr.py` | **Modify** (:319). Comment mention follows | T2 |
| `tests/test_tracearr_client.py` | **Modify** (:185). Docstring mention follows | T2 |
| `tests/conftest.py` | **Modify** (:24). Measured counts | T2 |
| `tests/test_ci_path_filters.py` | **Modify** (:385-386). Measured counts | T2 |
| `frontend/src/api/overrides.ts` | **Modify** (:138-153). M6's same-path join | T3 |
| `frontend/src/api/overrides.test.ts` | **Create.** M6's unit tests (RED naturally) | T3 |
| `frontend/src/pages/GroupsPanel.tsx` | **Modify** (:92-93, :365-367). M1 seeds; M5 badge move | T3 |
| `frontend/src/pages/GroupsPanel.test.tsx` | **Modify.** M5's DOM-order test (RED before the move) | T3 |
| `docs/superpowers/specs/2026-08-22-full-parity-roadmap.md` | **Modify.** Seven row closes | T4 |
| `.superpowers/sdd/p-sweep4-pr-body.md` | **Create** (gitignored, never committed) | T4 |
| `.superpowers/sdd/p-sweep4-runtime.md` | **Create** (gitignored) — row 131's measured table | T2 |

---

### Task 1: The Guards (rows 107, 109, 167, 118) — each seen RED

**Files:**
- Commit: `docs/superpowers/plans/2026-08-31-sweep-4.md`
- Modify: `tests/test_api_scheduled_runs.py` (append after the last test)
- Modify: `src/autoposter/api/routes.py:71-83`
- Modify: `frontend/src/pages/Library.test.tsx` (append inside `describe("Library")`)
- Modify: `tests/test_example_config_matches_schema.py` (append)
- Modify: `.forgejo/workflows/ci.yml:271-272`

**Interfaces:**
- Consumes: `SCHEDULED_JOB_NAMES` (`autoposter.api.routes`, a `frozenset[str]`); `Config.model_fields` (pydantic); the `Library.test.tsx` helpers `stubFetch`, `pathsMatching`, `SEARCH_DEBOUNCE_MS` already in that file.
- Produces: `test_the_run_now_allowlist_agrees_with_the_job_factories` (tests/test_api_scheduled_runs.py); `test_every_schema_section_appears_in_the_example` (tests/test_example_config_matches_schema.py); the Lint step reading `ruff check .`. Task 4 states suite counts against these additions (+2 backend tests, +1 frontend test).

- [ ] **Step 1: Cut the branch and commit this plan**

```bash
cd /d/Sites/autoposter
git fetch origin
git checkout fix/sweep-3
git checkout -b chore/sweep-4
# Content probe, never a sha probe (Global Constraint 8):
grep -q '_URL_HOST' src/autoposter/api/logs.py && echo PROBE-OK
```

Expected: `PROBE-OK`. If the probe fails, STOP — the base does not carry #108's content; report rather than guess.

```bash
git add docs/superpowers/plans/2026-08-31-sweep-4.md
git commit --no-gpg-sign -m "docs(plans): sweep 4 — the test/CI hygiene family"
```

- [ ] **Step 2: Row 107 — write the agreement guard**

Append to `tests/test_api_scheduled_runs.py` (module scope, after the last test; `re` joins the imports at the top of the file, `Path` is already imported there):

```python
def test_the_run_now_allowlist_agrees_with_the_job_factories():
    """Roadmap row 107. ``SCHEDULED_JOB_NAMES`` is spelled out in api/routes.py
    rather than imported from the factories (importing them would pull plexapi
    and the arr/http machinery into the route module), so the two lists can
    drift -- and a job missing from the allowlist silently cannot be
    hand-triggered. This reads the ``Job(name="...")`` literals out of both
    scheduler modules' source and demands exact agreement, both directions:
    a job the allowlist misses AND a stale allowlist name with no job both
    fail here.
    """
    import autoposter.scheduler.jobs as jobs_module
    import autoposter.scheduler.prune as prune_module
    from autoposter.api.routes import SCHEDULED_JOB_NAMES

    source = (
        Path(jobs_module.__file__).read_text(encoding="utf-8")
        + Path(prune_module.__file__).read_text(encoding="utf-8")
    )
    declared = set(re.findall(r'name="([a-z_]+)"', source))

    assert declared, "the regex found no Job(name=...) literals at all"
    assert declared == set(SCHEDULED_JOB_NAMES), (
        "the run-now allowlist and the job factories disagree -- "
        "missing from the allowlist: %s; stale in the allowlist: %s"
        % (sorted(declared - set(SCHEDULED_JOB_NAMES)),
           sorted(set(SCHEDULED_JOB_NAMES) - declared))
    )
```

Add `import re` to the imports at the top of the file (it has none today).

- [ ] **Step 3: Row 107 — see it RED (the drift is real)**

```bash
docker compose -p psw41 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_api_scheduled_runs.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t1-107-red.log; echo EXIT=\$?"
```

Expected: FAIL — `missing from the allowlist: ['credits_scan']`. This red is not manufactured: the drift already happened when the credits job shipped. Read the log from the host (`.superpowers/run-p-sweep4-t1-107-red.log`).

- [ ] **Step 4: Row 107 — fix the allowlist**

In `src/autoposter/api/routes.py:71-83`, replace the comment and set:

```python
# The names of the six periodic jobs, from the Job(name=...) literals in
# scheduler/jobs.py and scheduler/prune.py. Spelled out rather than imported
# from the job factories: importing those would pull plexapi and the arr/http
# client machinery into this module, which no request handler here needs. A
# name added there and not here can simply not be triggered by hand -- which
# is exactly what happened to credits_scan for a while, and what
# tests/test_api_scheduled_runs.py's agreement guard now catches in either
# direction.
SCHEDULED_JOB_NAMES = frozenset({
    "collections_reconcile",
    "ratings_drift_sweep",
    "credits_scan",
    "arr_sync",
    "asset_cleanup",
    "plex_prune",
})
```

- [ ] **Step 5: Row 107 — see it GREEN**

```bash
docker compose -p psw41 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_api_scheduled_runs.py tests/test_scheduler_prune_job.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t1-107-green.log; echo EXIT=\$?"
```

Expected: all pass, `EXIT=0` (the prune file's own per-job allowlist check at `test_scheduler_prune_job.py:852` must stay green too).

- [ ] **Step 6: Row 107 — commit**

```bash
git add tests/test_api_scheduled_runs.py src/autoposter/api/routes.py
git commit --no-gpg-sign -m "fix(api): run-now learns credits_scan, and a guard that the allowlist agrees with the factories"
```

- [ ] **Step 7: Row 109 — write the composition test**

Append inside `describe("Library", ...)` in `frontend/src/pages/Library.test.tsx`:

```tsx
  it("composes the search with the active filters in one listing URL", async () => {
    // Roadmap row 109: the listing effect builds ONE URLSearchParams from
    // library/kind/status/search together. The code is correct today; this
    // pins it so a dep-list regression in the effect -- a filter or the
    // debounced search silently dropped from the request -- goes red instead
    // of unseen.
    vi.useFakeTimers();
    try {
      const fetchMock = stubFetch();
      render(
        <MemoryRouter>
          <Library />
        </MemoryRouter>,
      );
      await act(async () => {});

      fireEvent.change(screen.getByLabelText("Library"), {
        target: { value: "Movies" },
      });
      await act(async () => {});
      fireEvent.change(screen.getByLabelText("Kind"), {
        target: { value: "movie" },
      });
      await act(async () => {});
      fireEvent.change(screen.getByLabelText("Status"), {
        target: { value: "rendered" },
      });
      await act(async () => {});
      fireEvent.change(screen.getByLabelText("Search"), {
        target: { value: "ghost" },
      });
      await act(async () => {
        vi.advanceTimersByTime(SEARCH_DEBOUNCE_MS);
      });
      await act(async () => {});

      const listings = pathsMatching(fetchMock, (path) =>
        path.startsWith("/api/items?"),
      );
      // The LAST listing carries everything at once, in the effect's own
      // build order -- limit/offset first, then the three selects, then the
      // debounced search.
      expect(listings.at(-1)).toBe(
        "/api/items?limit=48&offset=0&library=Movies&kind=movie&status=rendered&search=ghost",
      );
    } finally {
      vi.useRealTimers();
    }
  });
```

- [ ] **Step 8: Row 109 — see it RED by the row's own feared mutation**

Temporarily change `frontend/src/pages/Library.tsx:201` from

```tsx
  }, [library, kind, status, searchQuery, offset]);
```

to

```tsx
  }, [library, kind, status, offset]);
```

Run:

```bash
docker compose -p psw41 run --name psw41-109-red web npm test > .superpowers/run-p-sweep4-t1-109-red.log 2>&1
docker rm psw41-109-red
```

Expected: the new test FAILS (the final listing lacks `&search=ghost` — the debounce landed but the effect never refired). Then **restore line 201 exactly** and re-run:

```bash
docker compose -p psw41 run --name psw41-109-green web npm test > .superpowers/run-p-sweep4-t1-109-green.log 2>&1
docker rm psw41-109-green
```

Expected: 354 passed (353 baseline + this one). Confirm with `git diff frontend/src/pages/Library.tsx` → empty.

- [ ] **Step 9: Row 109 — commit**

```bash
git add frontend/src/pages/Library.test.tsx
git commit --no-gpg-sign -m "test(web): pin the search+filter composition in the Library listing URL"
```

- [ ] **Step 10: Row 167 — write the reverse schema test**

Append to `tests/test_example_config_matches_schema.py`:

```python
def test_every_schema_section_appears_in_the_example():
    """Roadmap row 167 -- the REVERSE walk. The three tests above check
    example -> schema, so a brand-new top-level Config section can ship with
    no line in the example and the suite stays green (the near-miss on
    record: deleting the whole ``tracearr:`` block reddened exactly one test
    in the entire suite, and only because it names ``tracearr`` by hand).
    Section level only, deliberately: a subkey-level reverse would red
    immediately on the many optional subkeys the example rightly omits,
    which is a different and larger decision.

    The ONE exemption: ``version`` is the derived render-version hash
    (config/loader.py's render_version), never operator-set, so the example
    deliberately omits it.
    """
    data = yaml.safe_load(EXAMPLE.read_text(encoding="utf-8"))
    missing = set(Config.model_fields) - set(data)

    assert missing == {"version"}, (
        "these schema sections have no line in the example config, so an "
        "operator cannot discover them: %s" % ", ".join(sorted(missing - {"version"}))
    )
```

- [ ] **Step 11: Row 167 — see it RED by the motivating mutation**

Temporarily delete the entire `tracearr:` block from `config/autoposter.example.yaml` (note its full text or rely on git to restore). Run:

```bash
docker compose -p psw41 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_example_config_matches_schema.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t1-167-red.log; echo EXIT=\$?"
```

Expected: the new test FAILS naming `tracearr`. Restore:

```bash
git checkout -- config/autoposter.example.yaml
docker compose -p psw41 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_example_config_matches_schema.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t1-167-green.log; echo EXIT=\$?"
```

Expected: 4 passed, `EXIT=0`.

- [ ] **Step 12: Row 167 — commit**

```bash
git add tests/test_example_config_matches_schema.py
git commit --no-gpg-sign -m "test(config): the example must name every schema section, version exempt"
```

- [ ] **Step 13: Row 118 — prove the current gate blind (RED), then widen it**

Plant a scratch violation outside the gate's paths:

```bash
printf 'import os\n' > alembic/red_proof_sweep4.py
docker compose -p psw41 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "ruff check src tests 2>&1 | tee /app/.superpowers/run-p-sweep4-t1-118-blind.log; echo EXIT=\$?"
```

Expected: `All checks passed!`, `EXIT=0` — the CI gate cannot see it.

```bash
docker compose -p psw41 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "ruff check . 2>&1 | tee /app/.superpowers/run-p-sweep4-t1-118-red.log; echo EXIT=\$?"
```

Expected: F401 on `alembic/red_proof_sweep4.py`, `EXIT=1` — the widened command sees it. Remove the plant:

```bash
rm alembic/red_proof_sweep4.py
docker compose -p psw41 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "ruff check . 2>&1 | tee /app/.superpowers/run-p-sweep4-t1-118-green.log; echo EXIT=\$?"
```

Expected: `All checks passed!`, `EXIT=0` (verified clean at plan time too).

- [ ] **Step 14: Row 118 — widen the gate**

In `.forgejo/workflows/ci.yml`, replace lines 271-272:

```yaml
      - name: Lint
        run: ruff check src tests
```

with:

```yaml
      - name: Lint
        # The whole checkout, not just src and tests: row 118's finding was
        # that `ruff check .` and the gate could disagree (two F401s in a
        # merge migration once failed the root command while CI stayed
        # green). Same paths for both commands means the disagreement is
        # structurally impossible, and alembic/ and scripts/ stop being
        # lint-free by accident.
        run: ruff check .
```

- [ ] **Step 15: Row 118 — commit**

```bash
git add .forgejo/workflows/ci.yml
git commit --no-gpg-sign -m "ci: the lint gate widens to ruff check . so root and gate agree"
```

- [ ] **Step 16: Task teardown**

```bash
docker compose -p psw41 down
```

---

### Task 2: The Cleanups (142), the Rename (152), the Re-derivation (131)

**Files:**
- Modify: `src/autoposter/config/schema.py` (:1-2 imports, :644-656 delete)
- Modify: `tests/test_collection_config.py` (:22 import; the three dead tests)
- Modify: `src/autoposter/collections/engine.py` (:54-60 import, :645-651 preview, :746 docstring)
- Test: `tests/test_builder_engine.py`
- Modify: `src/autoposter/collections/reconcile.py` (:452-496, :518-537, :1035-1048)
- Modify: `src/autoposter/collections/lists.py` (:370-385)
- Modify: `src/autoposter/collections/smart.py` (:419-432)
- Test: `tests/test_builder_settings.py`
- Rename: `src/autoposter/collections/imdb_lists.py` → `src/autoposter/collections/imdb_graphql.py` (+ the seven follower files listed in Step 10)
- Modify: `.forgejo/workflows/ci.yml` (:309-315 comment), `tests/conftest.py` (:24), `tests/test_ci_path_filters.py` (:385-386)
- Create: `.superpowers/sdd/p-sweep4-runtime.md` (gitignored)

**Interfaces:**
- Consumes: `resolve_collision(collection, label, adopt, adopt_from, remove_prior, dry_run, protect_labels) -> tuple[bool, str | None]` (`collections/reconcile.py:260`); `member_diff` (already imported in engine.py:53); the `tests/test_builder_engine.py` fakes (`FakeCollection`, `FakeSection`, `_Listing`, `registry_entry`, `run_library`, `_config`, `LABEL`); the `tests/test_builder_settings.py` fakes (`NoPlexPassCollection`, `_one_item_section`, `_run`, `LABEL`).
- Produces: `apply_collection_settings(...) -> tuple[list[str], bool]` and `_apply_hub(...) -> tuple[list[str], bool]` (changed signatures — every caller is updated in this task; no caller outside the three storing sites exists, verified at plan time); the module `autoposter.collections.imdb_graphql` exporting exactly what `imdb_lists` exported (`fetch_list`, `fetch_watchlist`, `fetch_search`, `LIST_QUERY`, `WATCHLIST_QUERY`, `SEARCH_QUERY`, `MAX_PAGES`, `PAGE_SIZE`, `ImdbListDrift`, `ImdbListRefused`). Task 4 counts on net +1 backend test here (+2 new, −3 deleted, and Task 1 added +2 → full-suite expectation 4182).

- [ ] **Step 1: 142(a) — delete the dead hash helper**

In `src/autoposter/config/schema.py`:
- Delete the whole function `definition_config_hash` (lines 644-656, the `def` through `return hashlib.sha256(...)`).
- Delete `import hashlib` (line 1) and `import json` (line 2) — verified at plan time these two are used nowhere else in the file.

In `tests/test_collection_config.py`:
- Remove `definition_config_hash,` from the import block at line 22.
- Delete the three tests that exist only to exercise it: the key-reorder-stability test containing `assert definition_config_hash(one) == definition_config_hash(other)` (~:176-192, including its docstring), `test_the_definition_hash_changes_when_a_setting_changes` (~:195-211), and the filters-fold test containing `assert definition_config_hash(base) != definition_config_hash(filtered)` (~:718-728, including its docstring).

In `src/autoposter/collections/engine.py:745-748`, the `_passing` docstring's closing sentence currently reads:

```
    The parse happens here rather than being carried on the definition
    because the model is dumped to JSON for ``definition_config_hash`` and
    copied by expansion, and neither would survive a compiled regex; it is
    microseconds against a pass that has just walked the library.
```

Replace with:

```
    The parse happens here rather than being carried on the definition
    because the model is copied by expansion, and a compiled regex would not
    survive the copy; it is microseconds against a pass that has just walked
    the library.
```

Verify nothing still references the name:

```bash
grep -rn "definition_config_hash" src tests --include="*.py"
```

Expected: no matches.

- [ ] **Step 2: 142(a) — run the touched suites and commit**

```bash
docker compose -p psw42 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_collection_config.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t2-142a.log; ruff check . 2>&1 | tail -3; echo EXIT=\$?"
```

Expected: all remaining tests pass; ruff clean (the two deleted imports would otherwise be F401 in reverse — deleting them with the function is what keeps it clean).

```bash
git add src/autoposter/config/schema.py tests/test_collection_config.py src/autoposter/collections/engine.py
git commit --no-gpg-sign -m "chore(config): delete the never-called definition_config_hash"
```

(If the engine.py preview edit from Step 3 is not yet made, the docstring edit still stands alone — commit exactly these three files.)

- [ ] **Step 3: 142(b) — write the failing preview-ownership test**

Append to `tests/test_builder_engine.py`, next to `test_the_preview_diff_is_taken_against_the_filtered_set` (~:1350):

```python
async def test_the_preview_counts_respect_ownership_resolution(
    session, registry_entry
):
    """Roadmap row 142(b). The preview's ``adding``/``removing`` used to be
    diffed against the current Plex listing BEFORE the ownership rule ran, so
    a collision-blocked collection -- one that exists under our title without
    our label -- previewed as if the write would apply when the pass will
    refuse it. The counts must be zero, and the conflict must still be
    reported exactly once (by the reconcile step's own dry-run message, not
    doubled by the preview block)."""
    registry_entry(_Listing(
        "test_preview_conflict", [("imdb", "tt1"), ("imdb", "tt2")]
    ))
    # No LABEL: this collection is somebody else's, and adopt is off in
    # ``_config()``.
    foreign = FakeCollection("Contested", [FakeItem("m9", ["imdb://tt9"])])
    section = FakeSection(
        [("m1", ["imdb://tt1"]), ("m2", ["imdb://tt2"])], existing=[foreign],
    )

    run = await run_library(
        session, section, "Movies", "Movie",
        [CollectionDefinition(title="Contested", builder="test_preview_conflict")],
        _config(), dry_run=True, preview=True,
    )

    [result] = run.definitions
    assert (result.adding, result.removing) == (0, 0), (
        "a collision-blocked collection must not preview a write the pass "
        "will refuse"
    )
    conflicts = [a for a in result.actions if "conflict" in a]
    assert len(conflicts) == 1, (
        "the conflict is reported exactly once -- by the reconcile step, "
        "not doubled by the preview block: %r" % result.actions
    )
```

- [ ] **Step 4: 142(b) — see it RED**

```bash
docker compose -p psw42 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_builder_engine.py::test_the_preview_counts_respect_ownership_resolution -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t2-142b-red.log; echo EXIT=\$?"
```

Expected: FAIL — `(result.adding, result.removing)` is `(2, 1)` today.

- [ ] **Step 5: 142(b) — implement**

In `src/autoposter/collections/engine.py:54-60`, add `resolve_collision` to the reconcile import:

```python
from autoposter.collections.reconcile import (
    LIBTYPES,
    has_label,
    load_labels,
    protected_label,
    reconcile_separator,
    resolve_collision,
)
```

Replace the preview block at :645-651:

```python
    if preview:
        collection = listing().get(definition.title)
        if collection is None:
            outcome.adding = len(items)
        elif items:
            adding, removing = member_diff(collection, items, definition.sync_mode)
            outcome.adding, outcome.removing = len(adding), len(removing)
```

with:

```python
    if preview:
        collection = listing().get(definition.title)
        if collection is None:
            outcome.adding = len(items)
        elif items:
            # Row 142(b): the same ownership rule the pass applies, read-only
            # (dry_run=True keeps ``claim_ownership`` unreached). A
            # collision-blocked collection must not preview a write the pass
            # will refuse. The message is dropped here on purpose:
            # ``reconcile_list_collection`` below runs the same check under
            # the same dry_run and reports it, and appending it twice would
            # double the action. Costs one ``collection.reload()`` GET per
            # previewed title that exists in Plex -- the per-title price the
            # real pass already pays in ``lists.py``.
            ok, _ = resolve_collision(
                collection, label,
                config.collections.adopt, config.collections.adopt_from,
                config.collections.adopt_removes_prior_label, True,
                config.collections.protect_labels,
            )
            if ok:
                adding, removing = member_diff(collection, items, definition.sync_mode)
                outcome.adding, outcome.removing = len(adding), len(removing)
```

- [ ] **Step 6: 142(b) — see it GREEN, alongside its neighbours**

```bash
docker compose -p psw42 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_builder_engine.py tests/test_builder_knobs.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t2-142b-green.log; echo EXIT=\$?"
```

Expected: all pass — including `test_the_preview_diff_is_taken_against_the_filtered_set`, whose existing collection DOES carry `LABEL` and therefore still gets its counts.

```bash
git add src/autoposter/collections/engine.py tests/test_builder_engine.py
git commit --no-gpg-sign -m "fix(collections): preview counts respect ownership resolution"
```

- [ ] **Step 7: 142(c) — write the failing hub-retry test**

Append to `tests/test_builder_settings.py`, next to `test_a_server_without_a_plex_pass_reports_and_carries_on` (~:680):

```python
async def test_a_failed_hub_write_is_retried_on_the_next_pass(
    session, registry_entry
):
    """Roadmap row 142(c). A failed ``_apply_hub`` used to let the definition
    hash store as current, so a Plex-Pass-required failure was never retried
    until the operator edited the definition. The hash now stores empty on a
    settings failure, so the next pass takes the update path again and the
    hub write is re-attempted -- observable as the failure action appearing
    on the SECOND pass too, where the old code short-circuited silently."""
    registry_entry(_Listing("settings_hub_retry", [("imdb", "tt1")]))
    live = NoPlexPassCollection("Pinned", labels=[LABEL])
    section = _one_item_section(existing=[live])
    definition = CollectionDefinition(
        title="Pinned", builder="settings_hub_retry", visible_home=True,
    )

    await _run(session, section, [definition])
    actions = await _run(session, section, [definition])

    assert any(
        "could not set the hub visibility of 'Pinned'" in a for a in actions
    ), "the second pass must retry the hub write, not skip on the stored hash"
```

- [ ] **Step 8: 142(c) — see it RED**

```bash
docker compose -p psw42 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_builder_settings.py::test_a_failed_hub_write_is_retried_on_the_next_pass -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t2-142c-red.log; echo EXIT=\$?"
```

Expected: FAIL — the second pass short-circuits on the stored hash and produces no hub action.

- [ ] **Step 9: 142(c) — implement the ok flag through the one shared seam**

In `src/autoposter/collections/reconcile.py`:

**(1)** `_apply_hub` (:452-496) returns `tuple[list[str], bool]`. Change the signature line and the three returns; the body is otherwise byte-identical:

```python
def _apply_hub(section, collection, definition) -> tuple[list[str], bool]:
```

- the early `return []` becomes `return [], True`
- the final `return actions` becomes `return actions, ok`, with the `except Exception` block setting a flag: add `ok = True` immediately before the `try:`, and inside the `except Exception:` block add `ok = False` as its first statement (before the `logger.exception`). Extend the docstring's second paragraph with one sentence: `The second half of the return says whether every write here landed -- a failed hub write must cost the definition a RETRY next pass (row 142c), and the callers gate the stored hash on it.`

**(2)** `apply_collection_settings` (:518-537) returns `tuple[list[str], bool]`:

```python
def apply_collection_settings(
    section, collection, definition, label: str, config
) -> tuple[list[str], bool]:
```

Body becomes:

```python
    if definition is None:
        return [], True
    hub_actions, hub_ok = _apply_hub(section, collection, definition)
    return [
        *_apply_labels(collection, definition, label, config),
        *_apply_sort_title(collection, definition),
        *_apply_mode(collection, definition),
        *hub_actions,
    ], hub_ok
```

Extend its docstring with: `Returns (actions, ok): ok is False when a setting could not be applied (today only the hub write can fail contained), and every caller that stores a definition hash stores "" instead of the real one on not-ok -- an empty string never equals a sha256 hexdigest, so the next pass re-reconciles and retries.`

**(3)** The bucket-family call site (:1035-1048): replace

```python
                actions += apply_collection_settings(
                    section, collection, bucket_settings, label, config
                )
```

with

```python
                settings_actions, settings_ok = apply_collection_settings(
                    section, collection, bucket_settings, label, config
                )
                actions += settings_actions
```

and in the store just below, `definition_hash=wanted,` (create branch) becomes `definition_hash=wanted if settings_ok else "",` and `record.definition_hash = wanted` (update branch) becomes `record.definition_hash = wanted if settings_ok else ""`.

In `src/autoposter/collections/lists.py` (:370-385): replace

```python
        if not dry_run and collection is not None:
            actions += apply_collection_settings(
                section, collection, settings, label, config
            )
            actions += _label_members(items, settings, title)
```

with

```python
        settings_ok = True
        if not dry_run and collection is not None:
            settings_actions, settings_ok = apply_collection_settings(
                section, collection, settings, label, config
            )
            actions += settings_actions
            actions += _label_members(items, settings, title)
```

and in the store below, `definition_hash=wanted,` becomes `definition_hash=wanted if settings_ok else "",` and `record.definition_hash = wanted` becomes `record.definition_hash = wanted if settings_ok else ""`.

In `src/autoposter/collections/smart.py` (:419-432): the same shape — replace

```python
            actions += apply_collection_settings(
                section, collection, settings, label, config
            )
```

with

```python
            settings_actions, settings_ok = apply_collection_settings(
                section, collection, settings, label, config
            )
            actions += settings_actions
```

(the store block at :423-432 sits in the same non-dry-run `else:` arm as this call, so the unpack binds `settings_ok` on every path that reaches it; still, initialize `settings_ok = True` immediately before the `if dry_run:` at :374 so the name is bound unconditionally and a future reshuffle cannot NameError), and gate the two stores exactly as in lists.py.

- [ ] **Step 10: 142(c) — see it GREEN, alongside every settings test**

```bash
docker compose -p psw42 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_builder_settings.py tests/test_collection_lists.py tests/test_collection_smart.py tests/test_collection_reconcile.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t2-142c-green.log; echo EXIT=\$?"
```

Expected: all pass. If any existing test asserts the stored `definition_hash` equals `wanted` after a hub-failure pass, it pins the bug — update it to expect `""` and say so in the commit body. Then:

```bash
git add src/autoposter/collections/reconcile.py src/autoposter/collections/lists.py \
  src/autoposter/collections/smart.py tests/test_builder_settings.py
git commit --no-gpg-sign -m "fix(collections): a failed hub write no longer stores the hash as current"
```

- [ ] **Step 11: Row 152 — the rename, one commit, nothing else in it**

```bash
git mv src/autoposter/collections/imdb_lists.py src/autoposter/collections/imdb_graphql.py
```

Then update every reference — the blast radius, enumerated exhaustively at plan time (nothing else in the tree names the transport; the builders module `collections/builders/imdb_lists.py` keeps its name deliberately — it holds exactly the two list builders it names):

1. `src/autoposter/collections/builders/imdb_lists.py:3` — docstring: `` `collections/imdb_lists.py` `` → `` `collections/imdb_graphql.py` ``
2. `src/autoposter/collections/builders/imdb_lists.py:53` — `from autoposter.collections.imdb_graphql import fetch_list, fetch_watchlist`
3. `src/autoposter/collections/builders/imdb_search.py:3` — same docstring mention
4. `src/autoposter/collections/builders/imdb_search.py:50` — `from autoposter.collections.imdb_graphql import fetch_search`
5. `tests/test_builder_imdb_lists.py:30` — `from autoposter.collections.imdb_graphql import (`
6. `tests/test_builder_imdb_search.py:6` — docstring mention → `imdb_graphql.py`
7. `tests/test_builder_imdb_search.py:35` — `from autoposter.collections.imdb_graphql import (`
8. `tests/test_builder_imdb_search.py:361` — the logger name follows `__name__`: `logger="autoposter.collections.imdb_graphql"`
9. `src/autoposter/providers/tmdb_lists.py:268` — comment: `` `imdb_lists._fetch` `` → `` `imdb_graphql._fetch` ``
10. `src/autoposter/providers/tmdb_lists.py:274` — comment: `` `imdb_lists` `` → `` `imdb_graphql` ``
11. `src/autoposter/providers/tracearr.py:319` — comment: `` ``collections/imdb_lists`` `` → `` ``collections/imdb_graphql`` ``
12. `tests/test_tracearr_client.py:185` — docstring: `` ``tmdb_lists``/``imdb_lists`` judgement `` → `` ``tmdb_lists``/``imdb_graphql`` judgement ``

The renamed module's own self-references at (old) :110, :362, :388 point at `builders/imdb_lists.py`, which keeps its name — leave them. Do NOT touch historical documents (old plans, closed roadmap rows, `.superpowers/` reports): they describe the tree as it was.

Verify the sweep is total:

```bash
grep -rn "collections.imdb_lists\|collections/imdb_lists" src tests --include="*.py" --include="*.ts" --include="*.tsx"
```

Expected: no matches.

- [ ] **Step 12: Row 152 — green, then the one commit**

```bash
docker compose -p psw42 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_builder_imdb_lists.py tests/test_builder_imdb_search.py tests/test_tracearr_client.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t2-152.log; ruff check . 2>&1 | tail -2; echo EXIT=\$?"
```

Expected: all pass, ruff clean. Confirm git tracked it as a rename (`git status` shows `renamed:`), then:

```bash
git add src/autoposter/collections/imdb_graphql.py src/autoposter/collections/imdb_lists.py \
  src/autoposter/collections/builders/imdb_lists.py src/autoposter/collections/builders/imdb_search.py \
  tests/test_builder_imdb_lists.py tests/test_builder_imdb_search.py \
  src/autoposter/providers/tmdb_lists.py src/autoposter/providers/tracearr.py \
  tests/test_tracearr_client.py
git commit --no-gpg-sign -m "refactor(collections): imdb_lists.py -> imdb_graphql.py, the transport names its three roots"
```

(`git add` on the old path records the deletion half of the `git mv`; listing both halves by name keeps the stage-by-name rule and the rename detection intact. `git log --follow src/autoposter/collections/imdb_graphql.py` must show the pre-rename history — check it.)

- [ ] **Step 13: Row 131 — the measured re-derivation**

Write `.superpowers/sdd/p-sweep4-runtime.md` with this content (the measured table is plan-time fact from this week's logs, all in `.superpowers/`; re-quote it verbatim):

```markdown
# Row 131 — suite runtime re-derived from this week's measured runs (2026-08-31)

Full backend suite (`pytest -q`, serial, compose `test` service), this week:

| log | duration | result |
| --- | --- | --- |
| run-p-sweep3-t1-full.log | 784.12s (13:04) | passed |
| run-p-sweep3-t2-full.log | 787.05s (13:07) | passed |
| run-p-sweep3-full.log    | 791.77s (13:11) | 4181 passed |
| run-p-sweep3-br-full.log | 790.53s (13:10) | 4181 passed |
| run-pdiv-t3-full.log     | 809.26s (13:29) | passed |
| run-pdiv-t3-full2.log    | 819.19s (13:39) | passed |
| run-pdiv-t5-full.log     | 826.81s (13:46) | passed |
| run-pdivbr-full.log      | 878.87s (14:38) | passed (week's worst) |

Derivation:
- FULL SUITE: `timeout -s KILL 2700` (3.07x the worst measured run). The
  row's filed guards -- 300 (SIGKILL at 34% of today's runtime, the row's
  own observed kill at 68%/91%) and the facts-file 600 (kills mid-run) --
  are both dead.
- TARGETED FILE: `timeout -s KILL 600` stays. Worst single-file green this
  week ~106s (run-pdiv-t3-green1b.log 103.83s) -- 5.6x headroom.
- CI: carries NO pytest timeout constant (the runner default governs);
  nothing to bump there. The stale exact-partition counts in its Test-step
  comment were refreshed instead (measured below).

Measured partition at this branch (filled in by the collect runs):
- collected total: <total>
- not imagemagick and not deep: <fast>
- deep: <deep>
- imagemagick: <magick>
- check: <fast> + <deep> + <magick> == <total>
```

Then measure the partition in-container and fill the four numbers in:

```bash
docker compose -p psw42 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "pytest tests/ --collect-only -q 2>&1 | tail -2 | tee /app/.superpowers/run-p-sweep4-t2-collect-all.log; \
  pytest tests/ --collect-only -q -m 'not imagemagick and not deep' 2>&1 | tail -2 | tee /app/.superpowers/run-p-sweep4-t2-collect-fast.log; \
  pytest tests/ --collect-only -q -m deep 2>&1 | tail -2 | tee /app/.superpowers/run-p-sweep4-t2-collect-deep.log; \
  pytest tests/ --collect-only -q -m imagemagick 2>&1 | tail -2 | tee /app/.superpowers/run-p-sweep4-t2-collect-magick.log"
```

The three numbers MUST partition the total exactly (fast + deep + imagemagick = collected). If they do not, STOP and report — a marker is leaking.

- [ ] **Step 14: Row 131 — refresh the three exactness-claiming counts**

Using the measured numbers (never the plan's — the executor's collect is the authority), update the counts in place, prose otherwise untouched:

1. `.forgejo/workflows/ci.yml` (:309-315, the Test step's "two lanes" comment): replace `513 of the 3,264 tests` with `<deep> of the <total> tests`, `2,740 tests` with `<fast> tests`, and `2,740 fast + 513 deferred + 11 ImageMagick-gated = 3,264 collected` with `<fast> fast + <deep> deferred + <magick> ImageMagick-gated = <total> collected`.
2. `tests/conftest.py:24`: replace `513 of the 3,253 non-ImageMagick tests` with `<deep> of the <fast+deep> non-ImageMagick tests`.
3. `tests/test_ci_path_filters.py:385-386`: replace `513 of the 3,264 tests` with `<deep> of the <total> tests`.

Then run the file that reads the workflow:

```bash
docker compose -p psw42 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "timeout -s KILL 600 pytest tests/test_ci_path_filters.py -q 2>&1 | tee /app/.superpowers/run-p-sweep4-t2-131.log; echo EXIT=\$?"
```

Expected: all pass (it asserts the lane selections, not the counts — verified at plan time; if a count assertion exists after all, it goes red here and gets the measured number).

```bash
git add .forgejo/workflows/ci.yml tests/conftest.py tests/test_ci_path_filters.py
git commit --no-gpg-sign -m "docs(ci): the lane partition counts re-measured, row 131's timeout audit on file"
docker compose -p psw42 down
```

---

### Task 3: The Frontend One-liners (M6, M1, M5)

**Files:**
- Create: `frontend/src/api/overrides.test.ts`
- Modify: `frontend/src/api/overrides.ts:138-153`
- Modify: `frontend/src/pages/GroupsPanel.tsx` (:92-93, :365-367)
- Modify: `frontend/src/pages/GroupsPanel.test.tsx` (append one test)

**Interfaces:**
- Consumes: `fieldErrors(detail: unknown): Record<string, string>` and `isPlainObject` from `frontend/src/api/overrides.ts`; the `GroupsPanel.test.tsx` helpers `renderPanel` and its GROUPS fixture (first group titled "Chart Collections").
- Produces: `fieldErrors` joining same-path messages with `"; "` (signature unchanged — the four consuming panels need no edit); the unsaved badge rendered before `.groups-style` in document order. Task 4 counts on +3 frontend tests (with Task 1's +1: 353 → 357, 21 → 22 files).

- [ ] **Step 1: M6 — write the failing unit tests**

Create `frontend/src/api/overrides.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { fieldErrors } from "./overrides";

/** M6 (the ccui review, deferred cross-phase): two 422 entries on the same
 * path used to collapse to the LAST one -- last-writer-wins hid every earlier
 * message for the field. Reachable for a multi-error body on a single-field
 * endpoint, and shared by all four panels that render `fieldErrors`. */
describe("fieldErrors", () => {
  it("joins two errors on the same path instead of keeping only the last", () => {
    const detail = [
      { path: "collections.definitions", message: "title is a duplicate" },
      { path: "collections.definitions", message: "the separator prefix is reserved" },
    ];

    expect(fieldErrors(detail)).toEqual({
      "collections.definitions":
        "title is a duplicate; the separator prefix is reserved",
    });
  });

  it("joins across the two entry shapes too", () => {
    // One handler-shaped entry, one FastAPI-shaped entry, same field.
    const detail = [
      { path: "plex.url", message: "not a URL" },
      { loc: ["body", "document", "plex", "url"], msg: "value error" },
    ];

    expect(fieldErrors(detail)).toEqual({
      "plex.url": "not a URL; value error",
    });
  });
});
```

- [ ] **Step 2: M6 — see them RED**

```bash
docker compose -p psw43 run --name psw43-m6-red web npm test > .superpowers/run-p-sweep4-t3-m6-red.log 2>&1
docker rm psw43-m6-red
```

Expected: both new tests FAIL (each object holds only the last message).

- [ ] **Step 3: M6 — the join**

In `frontend/src/api/overrides.ts`, replace the `fieldErrors` body (:138-153):

```ts
export function fieldErrors(detail: unknown): Record<string, string> {
  if (!Array.isArray(detail)) return {};
  const errors: Record<string, string> = {};
  // Joined, not last-writer-wins: two entries on one path both survive
  // (ccui review M6 -- the collapse hid every earlier message for a field).
  const add = (path: string, message: string) => {
    errors[path] = path in errors ? `${errors[path]}; ${message}` : message;
  };
  for (const entry of detail) {
    if (!isPlainObject(entry)) continue;
    if (typeof entry.path === "string") {
      add(entry.path, String(entry.message ?? "invalid value"));
    } else if (Array.isArray(entry.loc)) {
      const segments = entry.loc.map(String);
      const start = segments.indexOf("document");
      const path = (start === -1 ? segments : segments.slice(start + 1)).join(".");
      add(path, String(entry.msg ?? "invalid value"));
    }
  }
  return errors;
}
```

- [ ] **Step 4: M6 — green and commit**

```bash
docker compose -p psw43 run --name psw43-m6-green web npm test > .superpowers/run-p-sweep4-t3-m6-green.log 2>&1
docker rm psw43-m6-green
```

Expected: 356 passed, 22 files (353 + row 109's + these two) — every panel test still green (the four consumers render the joined string; no panel test pins a collapsed pair, verified by this run).

```bash
git add frontend/src/api/overrides.ts frontend/src/api/overrides.test.ts
git commit --no-gpg-sign -m "fix(web): fieldErrors keeps every message on a path, not just the last"
```

- [ ] **Step 5: M5 — write the failing DOM-order test**

Append inside the `describe` in `frontend/src/pages/GroupsPanel.test.tsx`:

```tsx
  it("shows the unsaved badge above the style block, not below the preview", async () => {
    // Divider review T4 M5: the badge used to render under the ~640px style
    // preview, a screen away from the buttons that made the order dirty.
    await renderPanel();

    fireEvent.click(
      screen.getByRole("button", { name: "Move Chart Collections down" }),
    );

    const badge = screen.getByText(/unsaved/i);
    const styleBlock = document.querySelector(".groups-style");
    expect(styleBlock).not.toBeNull();
    expect(
      badge.compareDocumentPosition(styleBlock!) &
        Node.DOCUMENT_POSITION_FOLLOWING,
    ).toBeTruthy();
  });
```

- [ ] **Step 6: M5 — RED, then the move (and M1's seeds in the same touch)**

```bash
docker compose -p psw43 run --name psw43-m5-red web npm test > .superpowers/run-p-sweep4-t3-m5-red.log 2>&1
docker rm psw43-m5-red
```

Expected: the new test FAILS (the badge follows the style block today).

In `frontend/src/pages/GroupsPanel.tsx`:

**(M5)** Move the badge block — delete lines 365-367:

```tsx
      {(dirty || styleDirty) && (
        <span className="groups-unsaved">unsaved — Save to store</span>
      )}
```

and insert the same three lines immediately BEFORE `<div className="groups-style">` (currently :307), i.e. between the last `groups-note` paragraph and the style block.

**(M1)** Lines 92-93: the two literal seeds go — the served value is the only source of a style name (the component's own stated rule):

```tsx
  const [savedStyle, setSavedStyle] = useState("");
  const [style, setStyle] = useState("");
```

(Unreachable before `adopt` runs — every render before it is behind the `groups === null` guard — so no test can see this; cosmetic by the T4 M1 filing, disclosed in the PR body.)

- [ ] **Step 7: M5/M1 — green, tsc, commit**

```bash
docker compose -p psw43 run --name psw43-m5-green web npm test > .superpowers/run-p-sweep4-t3-m5-green.log 2>&1
docker rm psw43-m5-green
docker compose -p psw43 run --name psw43-tsc web npx tsc --noEmit > .superpowers/run-p-sweep4-t3-tsc.log 2>&1
docker rm psw43-tsc
docker compose -p psw43 down
```

Expected: 357 passed, 22 files; tsc clean.

```bash
git add frontend/src/pages/GroupsPanel.tsx frontend/src/pages/GroupsPanel.test.tsx
git commit --no-gpg-sign -m "fix(web): the unsaved badge sits above the style preview, and the style seeds drop the literal"
```

---

### Task 4: Wrap — rows close, PR body, full suites stated-then-measured

**Files:**
- Modify: `docs/superpowers/specs/2026-08-22-full-parity-roadmap.md` (rows 107, 109, 118, 131, 142, 152, 167 — locate by row number, not line: this plan's cited lines 209/211/220/233/244/184/263 will have shifted)
- Create: `.superpowers/sdd/p-sweep4-pr-body.md` (gitignored, NEVER committed)

**Interfaces:**
- Consumes: every commit from Tasks 1-3; the run logs under `.superpowers/`; `.superpowers/sdd/p-sweep4-runtime.md` (Task 2's measured table).
- Produces: the closed rows; the PR body file. Nothing new is filed unless a review demands it (facts C3).

- [ ] **Step 1: State the expected counts BEFORE running (stated-then-measured)**

Write the statement into the wrap notes first, then measure. Expected:
- Backend full suite: **4182 passed** (baseline 4181 from `run-p-sweep3-full.log`; +107's test, +167's test, +142(b)'s, +142(c)'s = +4; −3 deleted with `definition_config_hash` = net +1).
- Frontend: **357 passed, 22 files** (353/21 baseline from `run-p-sweep3-web.log`; +109, +M6×2, +M5; +overrides.test.ts as the 22nd file).
- `ruff check .` clean; `tsc --noEmit` clean.

- [ ] **Step 2: Measure — the full backend suite, detached, no `--rm`**

```bash
docker compose -p psw44 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run -d --name psw44-full test sh -c "timeout -s KILL 2700 pytest tests/ -q 2>&1 | tee /app/.superpowers/run-p-sweep4-full.log; echo EXIT=\$? >> /app/.superpowers/run-p-sweep4-full.log"
docker wait psw44-full
tail -5 .superpowers/run-p-sweep4-full.log
docker rm psw44-full
```

Expected: `4182 passed in ~800s`, `EXIT=0` (the 2700 guard is Task 2's own derivation). Any deviation from the stated count is investigated by name, never waved through.

- [ ] **Step 3: Measure — frontend, tsc, ruff**

```bash
docker compose -p psw44 run --name psw44-web web npm test > .superpowers/run-p-sweep4-web.log 2>&1
docker rm psw44-web
docker compose -p psw44 run --name psw44-tsc web npx tsc --noEmit > .superpowers/run-p-sweep4-tsc.log 2>&1
docker rm psw44-tsc
docker compose -p psw44 -f docker-compose.yml -f .superpowers/isolated-db.yml \
  run --rm test sh -c "ruff check . 2>&1 | tee /app/.superpowers/run-p-sweep4-ruff.log; echo EXIT=\$?"
docker compose -p psw44 down
```

Expected: 357 passed / 22 files; tsc silent; `All checks passed!`.

- [ ] **Step 4: Close the seven rows**

In `docs/superpowers/specs/2026-08-22-full-parity-roadmap.md`, per the house pattern (title gains `(CLOSED — sweep 4)`, description gains a bold appendix; status columns untouched):

- **107**: append `**closed sweep-4:** the agreement guard reads the ``Job(name=...)`` literals out of both scheduler modules and demands exact two-way agreement with ``SCHEDULED_JOB_NAMES`` (tests/test_api_scheduled_runs.py). It landed RED against a real drift, not a manufactured one: ``credits_scan`` (registered at app.py:296) was missing from the allowlist, so the credits scan could not be hand-triggered — exactly the failure this row predicted. Fixed by adding it (a small additive behavior change: POST /api/scheduled-runs/credits_scan/run stops 404ing; the dashboard renders served rows, so no frontend edit).`
- **109**: append `**closed sweep-4:** one vitest pins the composed listing URL — library+kind+status+search in a single URLSearchParams — seen RED under the row's own feared mutation (``searchQuery`` dropped from the effect's dep list) before landing green (Library.test.tsx).`
- **118**: append `**closed sweep-4:** the stale-premise verdict above stands (both commands agree at HEAD), and the agreement is now structural rather than coincidental — the CI Lint step runs ``ruff check .``, the same command an operator runs at the root, proven by a planted out-of-gate F401 that the old gate could not see and the new one fails on.`
- **131**: append `**closed sweep-4:** re-derived from eight measured full runs this week (784–879s, worst run-pdivbr-full.log 878.87s at 4181 tests — the table in .superpowers/sdd/p-sweep4-runtime.md): full-suite recipe guard is now ``timeout -s KILL 2700`` (3.07x worst), targeted-file stays 600 (5.6x headroom over the worst ~106s single-file run). CI itself carries no pytest timeout constant — nothing to bump there. One disclosed widening: the same growth staled the exact-partition counts in ci.yml's Test-step comment, conftest.py:24 and test_ci_path_filters.py's docstring; refreshed from a measured collect in the same commit.`
- **142**: append `**closed sweep-4:** (a) ``definition_config_hash`` deleted with its three tests and two imports — called nowhere in src/autoposter; (b) preview's adding/removing now consult ``resolve_collision`` read-only before diffing, so a collision-blocked collection previews (0,0) with the conflict reported once by the reconcile step (cost: one reload GET per previewed existing title — the per-title price the real pass already pays); (c) a failed ``_apply_hub`` makes ``apply_collection_settings`` answer not-ok and all three storing sites store ``""`` instead of the definition hash, so the next pass retries — a permanently-Pass-less server with ``visible_*`` set now reports the failure every pass instead of falling silent forever, which is the disclosed cost of retrying at all.`
- **152**: append `**closed sweep-4:** renamed to ``imdb_graphql.py`` in its own one-rename commit (``git log --follow`` intact) — the module is the GraphQL transport serving three roots, and the new name says what it IS rather than naming its first tenant. Import path, one logger-name string (test_builder_imdb_search.py's caplog), and three comment mentions followed; ``builders/imdb_lists.py`` keeps its name on purpose (it holds exactly the two list builders it names).` — and the title's parenthetical updates from the deferral note to `(CLOSED — sweep 4)`.
- **167**: append `**closed sweep-4:** the section-level reverse test landed as sized (~8 lines): ``Config.model_fields`` minus the example's top-level keys must equal exactly ``{version}`` (the derived render-version hash, never operator-set — the one documented exemption). Seen RED under the row's own motivating mutation (the ``tracearr:`` block deleted from the example) before landing green.`

```bash
git add docs/superpowers/specs/2026-08-22-full-parity-roadmap.md
git commit --no-gpg-sign -m "docs(roadmap): sweep 4 closes the hygiene family (107/109/118/131/142/152/167)"
```

- [ ] **Step 5: Write the PR body (gitignored, not committed)**

Write `.superpowers/sdd/p-sweep4-pr-body.md`: title `chore: sweep 4 — the test/CI hygiene family (rows 107/109/118/131/142/152/167)`. Lead with the one real bug the sweep's guards caught: **the run-now allowlist had already drifted — `credits_scan` could not be hand-triggered, found by the row-107 guard landing red for real.** Then a section per row (107 the guard + the additive allowlist fix; 109 the composition pin and its mutation red; 118 the widened gate and the planted-F401 proof; 131 the measured table, the 2700/600 derivation, the disclosed partition-count refresh; 142's three fixes with (b)'s per-title reload cost and (c)'s retry-forever disclosure; 152 the rename with `--follow` intact; 167 the reverse walk and the `{version}` exemption). Then the frontend homes: M6's join (four panels share it), M5's badge move, M1's seeds — and **the one judged out: M3's aspect-ratio reflow stays homed at the divider review's UI polish pass** (needs the grid's measured ratio and a shared reserved box — not a one-liner). Then **Evidence** (the red logs by name, the stated-then-measured 4182/357 with log names) and **Stacking note**: this branch is THIRD in the merge order — cut from `fix/sweep-3` (#108); when #108 rebases onto main this branch rebases onto the rebased #108 before its own PR (second-to-merge-rebases, squared). Plain prose, no AI attribution anywhere in it. Do NOT commit this file; do NOT push; do NOT open a PR — the user gates that.

- [ ] **Step 6: Final verification sweep**

```bash
git log --oneline fix/sweep-3..HEAD
git status
```

Expected: ~13 commits, clean tree, `.superpowers/` untracked as always. Every run-*.log referenced above exists under `.superpowers/`. If a worktree was used anywhere despite the main-tree default, confirm its `run-*.log` files were copied out before pruning.
